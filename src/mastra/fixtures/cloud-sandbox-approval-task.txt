ROOT SANDBOX CONTRACT. Resume only after the durable protocol and isolated-workspace/integration workstreams are accepted. Start from the exact accepted mission integration head, but require the controller to allocate this job its own worker branch/workspace lineage; refuse to edit or push the shared integration branch. Preserve useful prior adapter work, but replace every host/local execution bypass with this fail-closed cloud boundary.

1. One provider-neutral interface: createWorkspace, uploadCredentiallessArchive, exec, read/write/list files, checkpoint, recreateFromCheckpoint, exportPatch, terminate. Persist separate providerWorkspaceId/providerSessionId on the fenced workAttempt. Trigger remains the durable executor; Convex remains authority; do not add another scheduler/control plane.

2. Daytona and E2B are the initial mature provider bake-off; add Sandbox0 as a first-class security/provider comparison and a Cloudflare Sandbox-compatible seam behind the exact same contract. Prefer Daytona only if its real managed-cloud contract proves the current snapshot/volume, network, archive and opaque-secret behavior; its $200 trial and 10-vCPU email-verified pool are attractive but not proof. If the selected provider key/config is absent or invalid, return a typed blocked/deferred state and checkpoint; NEVER execute on the Trigger host, VPS, localhost, `/home/ubuntu`, or a local Docker fallback. Do not claim any adapter active without real cloud credentials, plan/quota verification, lifecycle contract tests, and measured cold/warm cost.

   Pin the current E2B SDK and use its actual lifecycle/security surface: create with secure transport, no public traffic, empty envVars, `allow_internet_access: false` (or the equivalent denyOut 0.0.0.0/0 network rule), bounded timeout and auto-pause; reconnect paused sandboxes explicitly; kill terminal/orphaned sandboxes. Never persist envd/traffic access tokens in Convex, artifacts or logs. E2B's paused state can accelerate a retry but is not durable authority: paused sandboxes are retained indefinitely, so scheduled orphan cleanup and terminal kill are mandatory.

   Pin and contract-test Daytona's current TypeScript SDK independently: non-public sandbox, `networkBlockAll`/explicit domains, empty env, bounded resources and TTL, auto-stop/archive/delete, snapshot identity, persistent volume/subpath isolation, stop/archive/recreate, exact streamed command cancellation, and secret-placeholder non-disclosure. Container stop/archive and VM pause have different semantics; never imply memory survives a container stop. R2 remains the cross-provider content-addressed authority even when Daytona volumes/snapshots accelerate resume. The managed service is pay-as-you-go and the open-source stack is AGPL; do not self-host on the old VPS.

   Pin and contract-test the current Sandbox0 SDK separately. Use raw sandboxes as the tool boundary, not Sandbox0 Managed Agents: its managed Codex harness expects an OpenAI-compatible model-provider credential and must not introduce an OpenAI API model path. Evaluate persistent volume/rootfs checkpoint, recreate, warm-pool, block-all network, resource/TTL, and credential-projection behavior. Provider state remains a cache; R2 remains the portable checkpoint authority. Sandbox0 is v0.9.x/active beta, so any unsupported or ambiguous lifecycle operation must fail closed rather than be papered over.

3. Codex subscription/controller stays outside the sandbox. Run the pinned Codex app-server in a stripped, empty controller scratch environment and expose only controller-owned dynamic tools that call the sandbox adapter. No unrestricted built-in host shell/write path may reach a repository. The E2B process must never receive Codex auth, GitHub token, Convex/Trigger token, vault access, receipt-signing secret, provider deployment tokens, or inherited controller env. Prove this with spawned-process/environment tests.

4. Credentialless transport: controller hydrates the authorized Git ref, removes credentialed remotes/metadata, builds a bounded archive, uploads it, then receives a patch/artifact manifest bound to base SHA. Reject absolute paths, `..`, symlink/hardlink escapes, devices, oversized files/archives, changed base, binary surprises and secret-like output. Controller alone reviews/applies/commits/pushes through the delivery lease. Sandbox cannot reach GitHub directly.

   The E2B SDK has no atomic directory upload contract: write one already-validated archive through `files.write`, unpack only inside a fresh fixed workspace, and independently validate the exported archive/manifest again before controller application. Do not enumerate/upload a host directory file-by-file where a race can substitute files after validation.

5. Durable checkpoints: store immutable content-addressed archive/patch/session manifests in the app's R2 bucket with SHA-256, byte count, provider/template/runtime/lockfile identity and attempt causation. Convex stores compact refs/digests only. Recreate after preemption and replay the exact checkpoint; terminate idle/terminal sandboxes automatically. Never write large archives/transcripts to hot Convex job rows.

   E2B `pause()`/`connect()` preserves memory and filesystem and may be used between short segments, but every causal checkpoint must still be exportable to R2 and recreatable in a different sandbox id. Test both same-sandbox resume and new-sandbox recreation. Treat the provider's 1-hour Base/24-hour Pro continuous window as a segment limit, never as the mission lifetime.

6. Avoid repeated installs. Build/version E2B templates for the supported Node/Codex toolchain and use read-only dependency layers/caches keyed by runtime + exact lockfile digest. Cache population is controller-verified and project/tenant isolated; no cross-project writable `node_modules` or poisoned shared cache. On cache miss install once inside the sandbox, checkpoint the layer, then reuse. Report cold/warm timings and bytes.

7. Policy: network off/deny-by-default except explicitly scoped read-only endpoints required by the job; filesystem/resource/time/output limits; bounded command timeout/cancellation; automatic idle shutdown; spot/ephemeral cost controls where provider supports them. Money, messaging, publishing, ads, destructive operations and live commerce remain impossible without the existing Daniel gate.

8. Behavioral tests: fake provider contract; missing-key fail closed/no host spawn; secret/env absence; traversal/symlink/size/binary/base-SHA fences; create→exec→checkpoint→terminate→recreate exact replay; cancellation and stale-attempt fence; lockfile cache cold/warm counts; controller-only patch application; R2 digest mismatch; provider workspace/session separation. If a real E2B key is unavailable, report the integration test as explicitly blocked—not passed via a fallback.

9. Adapt, do not install, the useful Open SWE sandbox-backend patterns: one persistent sandbox lineage per thread/work item, automatic recreation behind a provider-neutral interface, full tool permission only inside the isolated boundary, and a deterministic message-queue check before each new model step so follow-up steering is incorporated without interrupting the always-available foreground Jarvis. Do not add LangGraph/Deep Agents, their model API path, Slack/Linear publication, or a second orchestration ledger. Also adapt Gas Town's important identity distinction: the durable agent/work item survives disposable sessions; provider sessions are replaceable transport, never the identity or authority.

Run full tests, typecheck, production build, diff/secret checks. Do not deploy, merge, push, open a provider pull request, or expose credentials from the worker. Commit only the isolated worker branch; the trusted controller alone may publish a reviewed ref for root review.
